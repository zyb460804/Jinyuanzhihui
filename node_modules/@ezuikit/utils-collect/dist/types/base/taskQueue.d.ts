import type API from './api';
declare class TaskQueue {
    /**
     * 是否停止fire
     */
    static isStop: boolean;
    /**
     * 待处理消息列表
     */
    static queues: Array<{
        url: string;
        data: object;
    }>;
    static api: API;
    /**
     * 添加消息
     * @param {string} url 上报url
     * @param {object} data 上报数据
     */
    static add(url: string, data: object): void;
    /**
     * 统一上报
     */
    static fire(): void;
}
export default TaskQueue;
