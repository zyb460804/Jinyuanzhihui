/**
 * 数据持久化
 */
declare class API {
    url: string;
    constructor(url: string);
    /**
     * 上报信息 （默认方式）
     */
    report(data: object): void;
    /**
     * 发送消息
     */
    sendInfo(data: object): void;
    sendImage(url: string, data: object): void;
    sendBeacon(url: string, data: object): void;
    formatParamsByURLSearchParams(data: any): URLSearchParams;
    changeJSON2Query(data: object): string;
    /**
     * 检测URL
     */
    checkUrl(url: string): boolean;
}
export default API;
